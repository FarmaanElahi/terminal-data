[project]
name = "terminal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.8",
    "typer>=0.23.0",
    "uvicorn[standard]>=0.40.0",
]

[project.scripts]
terminal = "terminal.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

